import Alert from "@mui/material/Alert";

const InfoAlert = () => (
  <Alert severity="info">
    For each activity, extract the data points defined in the output template(s)
    from the input files and upload the resulting csv file.
  </Alert>
);

export default InfoAlert;
