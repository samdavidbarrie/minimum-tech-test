import Alert from "@mui/material/Alert";

const InfoAlert = () => (
  <Alert severity="info">
    For each Activity, extract the datapoints definied in the output template(s)
    from the input files and upload the output Excel
  </Alert>
);

export default InfoAlert;
